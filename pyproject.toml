[project]
name = "advertisement"
version = "0.1.0"
description = "Default template for PDM package"
authors = [
    {name = "Azamat", email = "azamat_azamatov@epam.com"},
]
dependencies = ["uvicorn>=0.34.0", "fastapi>=0.115.6", "pymongo>=4.10.1"]
requires-python = ">=3.11"
readme = "README.md"
license = {text = "MIT"}

[build-system]
requires = ["pdm-backend"]
build-backend = "pdm.backend"


[tool.pdm]
distribution = true
